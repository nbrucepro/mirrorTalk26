package com.example.mirrortalk26;

/**
 * Paste your Anthropic API key here.
 *
 * ⚠  For production, move this to a backend proxy so the key is never
 *    shipped inside the APK. For development/demo this is fine.
 *
 * Get your key at: https://console.anthropic.com/
 */
public final class ApiKeyConfig {
    public static final String ANTHROPIC_API_KEY = "sk-ant-REPLACE_WITH_YOUR_KEY";

    private ApiKeyConfig() {}
}