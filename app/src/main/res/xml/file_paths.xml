<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!--
        Exposes getExternalFilesDir(null) as content:// URIs so VideoView
        and ShareCompat can open session recordings on Android 7+.
    -->
    <external-files-path
        name="session_videos"
        path="." />
</paths>